[project]
name = "sqlite-predict-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8",
  "syrupy>=4",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = "test_*.py"
