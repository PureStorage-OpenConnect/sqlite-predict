[project]
name = "sqlite-predict-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=9.1.1",
  "syrupy>=5.5.3",
  "sqlalchemy>=2.0.51",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = "test_*.py"
